package comSnoA12.harsha.object;

public class ObjectNotes {
	/*
	 * Agenda
	 * ******
	 * 
	 * 1.What is an object?
	 * Ans: Object is an instance of a class
	 * 		class is blueprint which contains -->variable 
	 * 										  -->Method   - we can't access directly
	 * 		If we want access it we have allocate memory in backEnd, we use the object creation
	 * 		with the help of object we allocate memory for class. what we allocated that memory access with object
	 * 		class 1 ---------> Objects (n)
	 * 
	 * 2.How do we create an object?
	 * Ans: Object creation
	 * 		***************
	 * --> class using
	 * --> constructor invoke is done
	 * --> variable & Methods are used
	 * --> Object created & stored in variable
	 * 
	 * 
	 * 3.What an object can contain in it?
	 * Ans: Variable & Methods
	 * 
	 * 4.How can we call the method or variables
	 * 		from within the class or Outside the class?
	 * Ans: checkOut in Test class how the Method & variables are call.
	 * 
	 * 
	 * 
	 * 
	 * 
	 * 
	 */
}
